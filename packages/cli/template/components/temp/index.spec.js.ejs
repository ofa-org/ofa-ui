import { describe, it, expect } from 'vitest'
import <%= componentName %> from './index.vue'

describe('<%= componentName %> Component', () => {
  it('1 should have the correct componentName', () => {
    expect(<%= componentName %>.name).toBe('Ofa<%= componentName %>')
  })
})
