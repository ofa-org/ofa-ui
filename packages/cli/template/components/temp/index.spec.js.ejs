import { describe, it, expect } from 'vitest'
import <%= name %> from './index.vue'

describe('<%= name %> Component', () => {
  it('1 should have the correct name', () => {
    expect(<%= name %>.name).toBe('Ofa<%= name %>')
  })
})
